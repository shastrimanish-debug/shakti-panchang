/*
import 'uma_final_engine.dart';
import 'uma_prediction_synthesis.dart';
import 'personalized_upay_engine.dart';

// Feed this from the real chart calculators, not hard-coded text.
final reading = const UmaFinalEngine().build(
  predictionContext: PredictionContext(
    signals: calculatedSignalsFromAllSystems,
  ),
  upayContext: UpayContext(
    rashi: calculatedRashi,
    mahadasha: calculatedMahadasha,
    antardasha: calculatedAntardasha,
    affectedGraha: affectedGrahas,
    affectedBhava: affectedBhavas,
    nakshatra: calculatedNakshatra,
    predictionCategory: category,
    predictionLevel: level,
  ),
);

// `reading.predictions` and `reading.upays` are now the single source for UMA UI.
*/
